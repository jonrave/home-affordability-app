# Browser-facing API base. The default uses the Next.js local proxy.
NEXT_PUBLIC_API_BASE=/api/backend

# Server-side target used by frontend/next.config.mjs for the local proxy.
BACKEND_API_URL=http://127.0.0.1:8000
